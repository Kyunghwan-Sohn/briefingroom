name: briefingroom

on:
  schedule:
    # 월~금 오전: KST 11:40 = UTC 02:40
    - cron: '40 2 * * 1-5'
    # 월~금 오후: KST 18:00 = UTC 09:00
    - cron: '0 9 * * 1-5'
    # 토요일 주간요약: KST 09:00 = UTC 00:00
    - cron: '0 0 * * 6'
    # 일요일 차주일정: KST 17:00 = UTC 08:00
    - cron: '0 8 * * 0'
  workflow_dispatch:
    inputs:
      date:
        description: 'YYYY-MM-DD (특정 날짜 수집)'
        required: false

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: briefing
  cancel-in-progress: false

jobs:
  briefing:
    runs-on: ubuntu-latest
    timeout-minutes: 120

    steps:
      - name: checkout
        uses: actions/checkout@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: python-setup
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: cache-playwright
        uses: actions/cache@v5
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ hashFiles('requirements.txt') }}

      - name: install
        run: |
          pip install -r requirements.txt
          playwright install chromium
          playwright install-deps chromium

      - name: verify-python
        run: python -m compileall briefing.py briefingroom

      - name: unit-tests
        run: python -m unittest discover -s tests -p 'test_*.py' -v

      - name: run
        env:
          WP_ENABLED: 'false'
          WP_URL: ${{ secrets.WP_URL }}
          WP_USER: ${{ secrets.WP_USER }}
          WP_PASS: ${{ secrets.WP_PASS }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_API_URL: ${{ secrets.LLM_API_URL }}
          LLM_MODEL: ${{ secrets.LLM_MODEL }}
          PROXY_URL: ${{ secrets.PROXY_URL }}
          CRAWL_DELAY_MIN: ${{ vars.CRAWL_DELAY_MIN || '30' }}
          CRAWL_DELAY_MAX: ${{ vars.CRAWL_DELAY_MAX || '90' }}
          TELEGRAM_ENABLED: 'true'
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          RUN_DATE: ${{ github.event.inputs.date || '' }}
          BRIEFING_SESSION: ${{ github.event.schedule == '40 2 * * 1-5' && 'am' || github.event.schedule == '0 9 * * 1-5' && 'pm' || '' }}
        run: python briefing.py

      - name: generate-pages
        env:
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_API_URL: ${{ secrets.LLM_API_URL }}
          LLM_MODEL: ${{ secrets.LLM_MODEL }}
        run: |
          echo "홈 페이지 생성..."
          python -m briefingroom.home_gen
          echo "finlaw 페이지 생성..."
          python -m briefingroom.finlaw_pages
          echo "입법예고 크롤링..."
          python -m briefingroom.legnotice

      - name: commit
        run: |
          git config user.name "briefing-bot"
          git config user.email "bot@briefingroom.kr"
          git add briefing.py briefingroom/ data/ feed/ articles/ finlaw/ index.html sitemap.xml .github/workflows/ tools/ policy/
          git diff --staged --quiet || git commit -m "briefing $(date +%Y-%m-%d) $(date +%H:%M)"
          git stash --include-untracked
          git pull --rebase origin main
          git stash pop || true
          if ! git push; then
            echo "push failed, retrying..."
            sleep 5
            git stash --include-untracked
            git pull --rebase origin main
            git stash pop || true
            git push
          fi

      - name: notify-failure
        if: failure()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          MSG="[govbrief.kr 장애] 크롤링 실패 - $(date +%Y-%m-%d' '%H:%M) KST%0A워크플로: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          if [ -n "$TELEGRAM_BOT_TOKEN" ] && [ -n "$TELEGRAM_CHAT_ID" ]; then
            curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
              -d "chat_id=${TELEGRAM_CHAT_ID}&text=${MSG}&parse_mode=HTML" > /dev/null
          fi
          echo "::error::브리핑룸 크롤링 실패 - $(date +%Y-%m-%d %H:%M)"
